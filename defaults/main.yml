---
group_name: '{{app}}'
group_priority: 10
servers: []
programs: '{{servers|join(",")}}'

supervisor_conf_tpl: _default_supervisor.conf.j2
supervisor_config_dir: /etc/supervisor/conf.d
supervisor_use_sudo: true
supervisor_ignore_handler_errors: no

supervisor_reload: true
